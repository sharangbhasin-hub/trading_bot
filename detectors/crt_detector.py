class CRTDetector:
    def detect_crt_candles(df, method='body_vs_wicks'):
        # Implements: body > wicks logic
        # Returns: DataFrame with CRT candle flags
    
    def calculate_50_percent_level(crt_high, crt_low):
        # Returns: Exact 50% level
