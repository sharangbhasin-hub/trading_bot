class KeyLevelDetector:
    def detect_ohp_olp(df, lookback=20):
        # Uses swing detection from liquidity_detector
        # Returns: OHP/OLP levels
    
    def detect_all_keylevels(df):
        # Combines: OHP, OLP, FVG, OB, RB
        # Returns: Dict of all detected levels
