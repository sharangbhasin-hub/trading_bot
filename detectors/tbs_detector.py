class TBSDetector:
    def detect_tbs_pattern(df, reference_level, direction):
        # Checks: First candle manipulating with body close
        # Validates: 2-6 candle patterns (A+ TBS)
        # Returns: TBS candle index and info
    
    def detect_model1(df_after_tbs, direction):
        # Finds: Thick opposite-direction candle
        # Max wait: Configurable (default 10 candles)
        # Returns: Model #1 candle info
